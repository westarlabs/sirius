sourceSets {
    test.resources.srcDirs += 'eth-contract'
    test.resources.includes += ['**/*.sol', '*.conf']
}

dependencies{
    compile project(":sirius-core")
    compile ('org.web3j:core:4.0.0')
}