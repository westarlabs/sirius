sourceSets {
    main.resources.srcDirs += 'eth-contract'
    main.resources.includes  += ['**/*.sol']

    test.resources.srcDirs += 'src/main/solidity'
    test.resources.includes += ['**/*.sol', '*.conf']
}

dependencies{
    compile project(":sirius-core")
}